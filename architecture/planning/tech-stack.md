

# Language, runtime, and platforms:

All implementation will be done with C#, and the native query languages (SQL) of the source/target database systems.

The runtime will start using dotnet 10 as a baseline, and the supported platforms should be any platform supported by the runtime and connection drivers that are in use.

