when developing new SQL or C# scripts, the user needs the option of providing connections and table mappings, and any other settings that apply to the script.  There needs to be an output window to show the actual SQL scripts that would be generated.  There should also be some standard fake connections / table mappings to help with the testing, they should only exist on these testing pages.